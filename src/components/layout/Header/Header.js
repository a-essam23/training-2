import Navbar from "./Navbar";
import Slide from "./Carousel";

const Header = (props) => {
  return (
    <div>
      <Navbar></Navbar>
      <Slide></Slide>
    </div>
  );
};

export default Header;
